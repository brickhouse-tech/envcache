name: release

# Disabled until repo is public — prevents auto-tagging which triggers publish
# on:
#   push:
#     branches: ["main"]
#     paths-ignore:
#       - '.devcontainer/**'
#     tags-ignore: ['**']

on:
  workflow_dispatch: # manual only for now

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
  tag-release:
    needs: [tests]
    uses: brickhouse-tech/.github/.github/workflows/release.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
